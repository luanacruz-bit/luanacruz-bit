<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>COMMENT.TRACKER // MARÇO</title>
<link href="https://fonts.googleapis.com/css2?family=Rajdhani:wght@300;400;500;600;700&family=Share+Tech+Mono&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #050508;
    --bg2: #080810;
    --surface: #0c0c18;
    --surface2: #10101e;
    --yellow: #f5e642;
    --cyan: #00d4ff;
    --pink: #ff2d78;
    --orange: #ff6b00;
    --text: #d0d8f0;
    --text2: #6a7090;
    --border: #1e1e35;
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: 'Rajdhani', sans-serif;
    min-height: 100vh;
    padding: 28px 18px 80px;
    position: relative;
    overflow-x: hidden;
    cursor: crosshair;
  }

  /* Scanlines overlay */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background: repeating-linear-gradient(
      0deg, transparent, transparent 2px,
      rgba(0,0,0,0.08) 2px, rgba(0,0,0,0.08) 4px
    );
    pointer-events: none;
    z-index: 9999;
  }

  /* Background grid */
  body::after {
    content: '';
    position: fixed;
    inset: 0;
    background-image:
      linear-gradient(rgba(0,212,255,0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(0,212,255,0.03) 1px, transparent 1px);
    background-size: 40px 40px;
    pointer-events: none;
    z-index: 0;
  }

  .container {
    max-width: 620px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  /* ── HEADER ── */
  .header { margin-bottom: 32px; }

  .sys-tag {
    font-family: 'Share Tech Mono', monospace;
    font-size: 10px;
    color: var(--cyan);
    letter-spacing: 3px;
    margin-bottom: 6px;
    opacity: 0.8;
  }

  .sys-tag::before { content: '> '; color: var(--yellow); }

  h1 {
    font-size: clamp(48px, 12vw, 80px);
    font-weight: 700;
    line-height: 0.9;
    letter-spacing: -1px;
    text-transform: uppercase;
    position: relative;
    display: inline-block;
  }

  h1 .line1 { color: var(--yellow); display: block; text-shadow: 0 0 30px rgba(245,230,66,0.4); }
  h1 .line2 { color: var(--text); display: block; font-weight: 300; }

  @keyframes glitch1 {
    0%, 90%, 100% { transform: translateX(0); opacity: 0; }
    92% { transform: translateX(-3px); opacity: 0.6; clip-path: polygon(0 30%, 100% 30%, 100% 50%, 0 50%); }
    94% { transform: translateX(3px); opacity: 0.4; clip-path: polygon(0 60%, 100% 60%, 100% 80%, 0 80%); }
    96% { transform: translateX(0); opacity: 0; }
  }

  .glitch-layer {
    position: absolute;
    top: 0; left: 0;
    color: var(--cyan);
    font-size: clamp(48px, 12vw, 80px);
    font-weight: 700;
    line-height: 0.9;
    text-transform: uppercase;
    animation: glitch1 4s infinite;
    pointer-events: none;
    opacity: 0;
  }

  .header-sub {
    font-family: 'Share Tech Mono', monospace;
    font-size: 11px;
    color: var(--text2);
    margin-top: 10px;
    letter-spacing: 2px;
  }

  .header-line {
    height: 1px;
    background: linear-gradient(90deg, var(--yellow), var(--cyan), transparent);
    margin-top: 16px;
  }

  /* ── CARD BASE ── */
  .card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 2px;
    padding: 22px;
    margin-bottom: 12px;
    position: relative;
  }

  .card::before {
    content: '';
    position: absolute;
    top: -1px; left: -1px;
    width: 16px; height: 16px;
    border-top: 2px solid var(--yellow);
    border-left: 2px solid var(--yellow);
  }

  .card::after {
    content: '';
    position: absolute;
    bottom: -1px; right: -1px;
    width: 16px; height: 16px;
    border-bottom: 2px solid var(--cyan);
    border-right: 2px solid var(--cyan);
  }

  .card-label {
    font-family: 'Share Tech Mono', monospace;
    font-size: 9px;
    letter-spacing: 3px;
    color: var(--cyan);
    text-transform: uppercase;
    margin-bottom: 14px;
    opacity: 0.7;
  }

  /* ── MAIN PROGRESS ── */
  .main-progress {
    padding: 24px;
    overflow: hidden;
  }

  .side-accent {
    position: absolute;
    top: 0; left: 0;
    width: 3px; height: 100%;
    background: linear-gradient(180deg, var(--yellow), transparent);
  }

  .prog-top {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 18px;
  }

  .prog-count {
    font-size: 80px;
    font-weight: 700;
    line-height: 1;
    letter-spacing: -3px;
    color: var(--yellow);
    text-shadow: 0 0 30px rgba(245,230,66,0.4), 0 0 60px rgba(245,230,66,0.1);
  }

  .prog-count span {
    font-size: 28px;
    color: var(--text2);
    font-weight: 300;
    letter-spacing: 0;
  }

  .prog-right { text-align: right; }

  .pct-display {
    font-size: 36px;
    font-weight: 700;
    color: var(--cyan);
    text-shadow: 0 0 20px rgba(0,212,255,0.5);
    line-height: 1;
  }

  .pct-label {
    font-family: 'Share Tech Mono', monospace;
    font-size: 9px;
    color: var(--text2);
    letter-spacing: 2px;
  }

  .seg-bar {
    display: flex;
    gap: 2px;
    margin-bottom: 10px;
  }

  .seg {
    flex: 1;
    height: 8px;
    background: var(--surface2);
    border-radius: 1px;
    transition: background 0.3s ease, box-shadow 0.3s ease;
  }

  .seg.filled { background: var(--yellow); box-shadow: 0 0 6px rgba(245,230,66,0.6); }
  .seg.recent { background: var(--cyan); box-shadow: 0 0 8px rgba(0,212,255,0.9); animation: flicker 2s infinite; }

  @keyframes flicker { 0%,95%,100%{opacity:1;} 97%{opacity:0.5;} }

  .prog-meta {
    display: flex;
    justify-content: space-between;
    font-family: 'Share Tech Mono', monospace;
    font-size: 10px;
    color: var(--text2);
  }

  .prog-meta .alert { color: var(--pink); }
  .prog-meta .ok { color: var(--cyan); }

  /* ── STATS GRID ── */
  .stats-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
    margin-bottom: 12px;
  }

  .stat-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 2px;
    padding: 16px 18px;
    position: relative;
    overflow: hidden;
    transition: border-color 0.2s;
  }

  .stat-card::before {
    content: '';
    position: absolute;
    top: 0; left: 0;
    width: 100%; height: 2px;
    opacity: 0;
    transition: opacity 0.2s;
  }

  .stat-card:hover { border-color: var(--yellow); }
  .stat-card:hover::before { opacity: 1; background: var(--yellow); }

  .stat-lbl {
    font-family: 'Share Tech Mono', monospace;
    font-size: 8px;
    letter-spacing: 3px;
    color: var(--text2);
    text-transform: uppercase;
    margin-bottom: 8px;
  }

  .stat-val {
    font-size: 36px;
    font-weight: 700;
    line-height: 1;
    letter-spacing: -1px;
  }

  .y { color: var(--yellow); text-shadow: 0 0 15px rgba(245,230,66,0.3); }
  .c { color: var(--cyan); text-shadow: 0 0 15px rgba(0,212,255,0.3); }
  .p { color: var(--pink); text-shadow: 0 0 15px rgba(255,45,120,0.3); }
  .w { color: var(--text); }

  .stat-sub {
    font-family: 'Share Tech Mono', monospace;
    font-size: 9px;
    color: var(--text2);
    margin-top: 4px;
    letter-spacing: 1px;
  }

  /* ── TODAY ── */
  .today-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 2px;
    padding: 22px;
    margin-bottom: 12px;
    position: relative;
  }

  .today-card::before {
    content: '';
    position: absolute;
    top: 0; left: 0;
    width: 100%; height: 2px;
    background: linear-gradient(90deg, var(--yellow), var(--orange), var(--pink));
  }

  .today-card-corner {
    position: absolute;
    bottom: -1px; right: -1px;
    width: 16px; height: 16px;
    border-bottom: 2px solid var(--cyan);
    border-right: 2px solid var(--cyan);
  }

  .today-top {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    margin-bottom: 20px;
  }

  .today-big {
    font-size: 64px;
    font-weight: 700;
    line-height: 1;
    letter-spacing: -2px;
    color: var(--yellow);
    text-shadow: 0 0 30px rgba(245,230,66,0.5);
    transition: all 0.3s;
  }

  .today-info { text-align: right; }

  .today-date-lbl {
    font-size: 22px;
    font-weight: 700;
    color: var(--cyan);
    line-height: 1;
  }

  .today-target-lbl {
    font-family: 'Share Tech Mono', monospace;
    font-size: 9px;
    color: var(--text2);
    letter-spacing: 2px;
    margin-top: 4px;
  }

  .today-lbl-sm {
    font-family: 'Share Tech Mono', monospace;
    font-size: 9px;
    color: var(--text2);
    letter-spacing: 3px;
    margin-bottom: 4px;
  }

  /* Input row */
  .input-row {
    display: flex;
    gap: 8px;
    align-items: stretch;
    margin-bottom: 12px;
  }

  .qty-wrap {
    display: flex;
    align-items: center;
    background: var(--surface2);
    border: 1px solid var(--border);
    border-radius: 2px;
    overflow: hidden;
  }

  .qty-btn {
    background: transparent;
    border: none;
    color: var(--yellow);
    font-family: 'Rajdhani', sans-serif;
    font-size: 20px;
    font-weight: 700;
    width: 36px;
    height: 100%;
    cursor: pointer;
    transition: background 0.15s;
    display: flex; align-items: center; justify-content: center;
  }

  .qty-btn:hover { background: rgba(245,230,66,0.08); }

  .qty-input {
    background: transparent;
    border: none;
    color: var(--yellow);
    font-family: 'Share Tech Mono', monospace;
    font-size: 22px;
    width: 60px;
    text-align: center;
    outline: none;
    -moz-appearance: textfield;
    padding: 12px 0;
  }

  .qty-input::-webkit-outer-spin-button,
  .qty-input::-webkit-inner-spin-button { -webkit-appearance: none; }

  .confirm-btn {
    flex: 1;
    background: var(--yellow);
    color: #050508;
    border: none;
    border-radius: 2px;
    font-family: 'Rajdhani', sans-serif;
    font-size: 17px;
    font-weight: 700;
    letter-spacing: 3px;
    text-transform: uppercase;
    cursor: pointer;
    transition: all 0.15s;
    position: relative;
    overflow: hidden;
    clip-path: polygon(10px 0%, 100% 0%, calc(100% - 10px) 100%, 0% 100%);
  }

  .confirm-btn::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
    transform: translateX(-100%);
    transition: transform 0.4s;
  }

  .confirm-btn:hover::before { transform: translateX(100%); }
  .confirm-btn:hover {
    box-shadow: 0 0 24px rgba(245,230,66,0.6), 0 0 50px rgba(245,230,66,0.2);
    transform: translateY(-1px);
  }
  .confirm-btn:active { transform: translateY(0); }

  .confirm-btn.done {
    background: var(--surface2);
    color: var(--text2);
    cursor: default;
    clip-path: none;
    border: 1px solid var(--border);
    box-shadow: none;
    transform: none;
  }

  .confirm-btn.done::before { display: none; }
  .confirm-btn.done:hover { box-shadow: none; transform: none; }

  /* Balance */
  .balance-card {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 12px 16px;
    border-radius: 2px;
    font-family: 'Share Tech Mono', monospace;
    font-size: 11px;
    letter-spacing: 1px;
    transition: all 0.4s;
    border-left: 3px solid transparent;
  }

  .balance-card.neutral { background: var(--surface2); border-left-color: var(--text2); color: var(--text2); }
  .balance-card.pos { background: rgba(0,212,255,0.05); border-left-color: var(--cyan); color: var(--cyan); box-shadow: inset 0 0 20px rgba(0,212,255,0.03); }
  .balance-card.neg { background: rgba(255,45,120,0.05); border-left-color: var(--pink); color: var(--pink); box-shadow: inset 0 0 20px rgba(255,45,120,0.03); }

  .bal-icon { font-size: 16px; flex-shrink: 0; }

  /* ── HISTORY ── */
  .history-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 2px;
    padding: 20px 22px;
    margin-bottom: 12px;
    position: relative;
  }

  .history-card::before {
    content: '';
    position: absolute;
    top: -1px; left: -1px;
    width: 16px; height: 16px;
    border-top: 2px solid var(--yellow);
    border-left: 2px solid var(--yellow);
  }

  .history-title {
    font-family: 'Share Tech Mono', monospace;
    font-size: 9px;
    letter-spacing: 4px;
    color: var(--yellow);
    margin-bottom: 16px;
    opacity: 0.8;
  }

  .history-list { display: flex; flex-direction: column; gap: 6px; }

  .hist-item {
    display: grid;
    grid-template-columns: 60px 1fr 44px;
    align-items: center;
    gap: 10px;
    padding: 8px 12px;
    background: var(--surface2);
    border-radius: 1px;
    border-left: 2px solid transparent;
    transition: background 0.2s;
  }

  .hist-item.good { border-left-color: var(--cyan); }
  .hist-item.bad { border-left-color: var(--pink); }
  .hist-item:hover { background: #14141f; }

  .hist-day { font-family: 'Share Tech Mono', monospace; font-size: 10px; color: var(--text2); }

  .hist-bar-wrap { height: 3px; background: var(--border); border-radius: 1px; overflow: hidden; }

  .hist-bar { height: 100%; border-radius: 1px; transition: width 0.5s ease; }
  .hist-bar.good { background: linear-gradient(90deg, var(--cyan), var(--yellow)); box-shadow: 0 0 4px rgba(0,212,255,0.6); }
  .hist-bar.bad { background: var(--pink); box-shadow: 0 0 4px rgba(255,45,120,0.6); }

  .hist-count { font-family: 'Share Tech Mono', monospace; font-size: 13px; text-align: right; }
  .hist-count.good { color: var(--cyan); }
  .hist-count.bad { color: var(--pink); }

  .empty-hist {
    font-family: 'Share Tech Mono', monospace;
    font-size: 10px;
    color: var(--text2);
    text-align: center;
    padding: 14px;
    letter-spacing: 2px;
    opacity: 0.6;
  }

  /* ── FOOTER ── */
  .footer-row { display: flex; justify-content: space-between; align-items: center; }

  .reset-btn {
    background: transparent;
    border: 1px solid var(--border);
    border-radius: 1px;
    color: var(--text2);
    font-family: 'Share Tech Mono', monospace;
    font-size: 9px;
    letter-spacing: 2px;
    padding: 7px 14px;
    cursor: pointer;
    text-transform: uppercase;
    transition: all 0.2s;
  }

  .reset-btn:hover { border-color: var(--pink); color: var(--pink); }

  .sys-status {
    font-family: 'Share Tech Mono', monospace;
    font-size: 9px;
    color: var(--text2);
    letter-spacing: 2px;
    opacity: 0.5;
  }

  .sys-status::before {
    content: '● ';
    color: var(--cyan);
    animation: blink 1.5s step-end infinite;
  }

  @keyframes blink { 0%,100%{opacity:1;} 50%{opacity:0;} }

  /* Decorative corner numbers */
  .deco-num {
    position: fixed;
    font-family: 'Share Tech Mono', monospace;
    font-size: 9px;
    color: rgba(0,212,255,0.1);
    letter-spacing: 2px;
    pointer-events: none;
    z-index: 2;
  }

  .deco-tl { top: 12px; left: 12px; }
  .deco-br { bottom: 12px; right: 12px; }

  @keyframes fadeSlideIn {
    from { opacity: 0; transform: translateX(-8px); }
    to { opacity: 1; transform: translateX(0); }
  }

  .fade-in { animation: fadeSlideIn 0.35s ease forwards; }

  @keyframes glowPulse {
    0% { text-shadow: 0 0 30px rgba(245,230,66,0.4); }
    50% { text-shadow: 0 0 60px rgba(245,230,66,1), 0 0 100px rgba(245,230,66,0.4); }
    100% { text-shadow: 0 0 30px rgba(245,230,66,0.4); }
  }

  .glow-pulse { animation: glowPulse 0.6s ease; }
</style>
</head>
<body>

<div class="deco-num deco-tl">SYS::TRACKER_v2.077</div>
<div class="deco-num deco-br">NIGHT CITY // MÊS 03</div>

<div class="container">

  <header class="header">
    <div class="sys-tag">COMMENT_TRACKER.EXE // MARÇO 2026</div>
    <div style="position:relative;display:inline-block;">
      <h1>
        <span class="line1">500</span>
        <span class="line2">COMENTÁRIOS</span>
      </h1>
      <div class="glitch-layer">
        <span class="line1">500</span>
        <span class="line2">COMENTÁRIOS</span>
      </div>
    </div>
    <div class="header-sub">[ META: 20–30/DIA · 31 DIAS · VOCÊ CONSEGUE ]</div>
    <div class="header-line"></div>
  </header>

  <!-- MAIN PROGRESS -->
  <div class="card main-progress">
    <div class="side-accent"></div>
    <div class="card-label">// status geral</div>
    <div class="prog-top">
      <div>
        <div class="prog-count" id="total-done">0<span>/500</span></div>
      </div>
      <div class="prog-right">
        <div class="pct-display" id="pct-display">0%</div>
        <div class="pct-label">COMPLETO</div>
      </div>
    </div>
    <div class="seg-bar" id="seg-bar"></div>
    <div class="prog-meta">
      <span id="remaining-lbl" class="ok">500 FALTANDO</span>
      <span id="days-lbl">DIA 1 DE 31</span>
    </div>
  </div>

  <!-- STATS -->
  <div class="stats-grid">
    <div class="stat-card">
      <div class="stat-lbl">// necessário/dia</div>
      <div class="stat-val y" id="needed-val">17</div>
      <div class="stat-sub">PRA BATER 500</div>
    </div>
    <div class="stat-card">
      <div class="stat-lbl">// projeção final</div>
      <div class="stat-val c" id="proj-val">—</div>
      <div class="stat-sub">NO RITMO ATUAL</div>
    </div>
    <div class="stat-card">
      <div class="stat-lbl">// melhor dia</div>
      <div class="stat-val y" id="best-val">—</div>
      <div class="stat-sub" id="best-sub">SEM DADOS</div>
    </div>
    <div class="stat-card">
      <div class="stat-lbl">// média diária</div>
      <div class="stat-val w" id="avg-val">—</div>
      <div class="stat-sub">DIAS REGISTRADOS</div>
    </div>
  </div>

  <!-- TODAY -->
  <div class="today-card">
    <div class="today-card-corner"></div>
    <div class="today-top">
      <div>
        <div class="today-lbl-sm">// HOJE</div>
        <div class="today-big" id="today-count">0</div>
      </div>
      <div class="today-info">
        <div class="today-date-lbl" id="today-date-lbl">01/03</div>
        <div class="today-target-lbl">META: 20 — 30</div>
      </div>
    </div>

    <div class="input-row">
      <div class="qty-wrap">
        <button class="qty-btn" onclick="changeQty(-5)">−</button>
        <input class="qty-input" type="number" id="qty-input" value="25" min="1" max="200">
        <button class="qty-btn" onclick="changeQty(5)">+</button>
      </div>
      <button class="confirm-btn" id="confirm-btn" onclick="confirmToday()">
        ◆ CONFIRMAR
      </button>
    </div>

    <div class="balance-card neutral" id="balance-card">
      <span class="bal-icon">◈</span>
      <span id="balance-text">AGUARDANDO CONFIRMAÇÃO DO DIA...</span>
    </div>
  </div>

  <!-- HISTORY -->
  <div class="history-card">
    <div class="history-title">// LOG DE MARÇO</div>
    <div class="history-list" id="history-list">
      <div class="empty-hist">[ NENHUM REGISTRO ENCONTRADO ]</div>
    </div>
  </div>

  <div class="footer-row">
    <button class="reset-btn" onclick="resetData()">↺ RESETAR DADOS</button>
    <div class="sys-status">ONLINE</div>
  </div>

</div>

<script>
  const GOAL=500, DAYS=31, MIN=20, MAX=30, MID=25, SEGS=25;

  let state = JSON.parse(localStorage.getItem('cp-tracker')||'null') || { history:{} };
  function save(){ localStorage.setItem('cp-tracker', JSON.stringify(state)); }

  function getToday(){
    const d=new Date();
    if(d.getMonth()===2&&d.getFullYear()===2026) return d.getDate();
    return 1;
  }

  function total(){ return Object.values(state.history).reduce((a,b)=>a+b,0); }
  function logged(){ return Object.keys(state.history).length; }
  function avg(){ const l=logged(); return l?Math.round(total()/l):0; }
  function needed(){ const t=getToday(),r=GOAL-total(),d=DAYS-t+1; return d>0?Math.ceil(r/d):0; }
  function projection(){ const a=avg(),t=getToday(),dl=DAYS-t; return total()+a*dl; }
  function bestDay(){ const e=Object.entries(state.history); return e.length?e.reduce((b,c)=>c[1]>b[1]?c:b):null; }
  function fmt(n){ return n.toString().padStart(2,'0')+'/03'; }

  function buildSegBar(){
    const bar=document.getElementById('seg-bar');
    const t=total();
    const filled=Math.round(t/GOAL*SEGS);
    bar.innerHTML='';
    for(let i=0;i<SEGS;i++){
      const s=document.createElement('div');
      if(i<filled) s.className = i===filled-1?'seg recent':'seg filled';
      else s.className='seg';
      bar.appendChild(s);
    }
  }

  function render(){
    const today=getToday(), t=total(), rem=Math.max(0,GOAL-t), pct=Math.min(100,Math.round(t/GOAL*100));
    const todayCount=state.history[today]||0, l=logged();

    document.getElementById('today-date-lbl').textContent=fmt(today);
    document.getElementById('today-count').textContent=todayCount;
    document.getElementById('total-done').innerHTML=`${t}<span>/500</span>`;
    document.getElementById('pct-display').textContent=pct+'%';
    buildSegBar();

    const remEl=document.getElementById('remaining-lbl');
    remEl.textContent=rem>0?`${rem} FALTANDO`:'🏆 META BATIDA!';
    remEl.className=rem===0?'ok':'alert';
    document.getElementById('days-lbl').textContent=`DIA ${today} DE ${DAYS}`;

    const n=needed(), nEl=document.getElementById('needed-val');
    nEl.textContent=n>0?n:'✓';
    nEl.className='stat-val '+(n<=MAX?'y':'p');

    if(l>0){
      const p=projection(), pEl=document.getElementById('proj-val');
      pEl.textContent=p; pEl.className='stat-val '+(p>=GOAL?'c':'p');
    }

    const best=bestDay();
    if(best){ document.getElementById('best-val').textContent=best[1]; document.getElementById('best-sub').textContent=`DIA ${best[0]}/03`; }

    const a=avg(), aEl=document.getElementById('avg-val');
    if(a>0){ aEl.textContent=a; aEl.className='stat-val '+(a>=MIN?'c':'p'); }

    const btn=document.getElementById('confirm-btn');
    if(state.history[today]!==undefined){ btn.className='confirm-btn done'; btn.textContent='✓ REGISTRADO'; }
    else { btn.className='confirm-btn'; btn.textContent='◆ CONFIRMAR'; }

    renderBalance(today);
    renderHistory(today);
  }

  function renderBalance(today){
    const card=document.getElementById('balance-card');
    const text=document.getElementById('balance-text');
    const icon=card.querySelector('.bal-icon');
    if(state.history[today]===undefined){
      card.className='balance-card neutral'; icon.textContent='◈';
      text.textContent='AGUARDANDO CONFIRMAÇÃO DO DIA...'; return;
    }
    const tc=state.history[today], diff=tc-MID, rem=Math.max(0,GOAL-total());
    const daysLeft=DAYS-today, tmr=daysLeft>0?Math.ceil(rem/daysLeft):0;
    if(diff>0){ card.className='balance-card pos'; icon.textContent='▲'; text.textContent=`+${diff} ACIMA DA META · AMANHÃ: ${tmr} NECESSÁRIOS · ${rem} RESTANTES`; }
    else if(diff<0){ card.className='balance-card neg'; icon.textContent='▼'; text.textContent=`${diff} ABAIXO · AMANHÃ VOCÊ PRECISA DE ${tmr} · ${rem} RESTANTES`; }
    else { card.className='balance-card pos'; icon.textContent='◆'; text.textContent=`NA META EXATA · AMANHÃ: ${tmr} NECESSÁRIOS · ${rem} RESTANTES`; }
  }

  function renderHistory(today){
    const list=document.getElementById('history-list');
    const entries=Object.entries(state.history).map(([k,v])=>[parseInt(k),v]).sort((a,b)=>b[0]-a[0]);
    if(!entries.length){ list.innerHTML='<div class="empty-hist">[ NENHUM REGISTRO ENCONTRADO ]</div>'; return; }
    list.innerHTML=entries.map(([day,count])=>{
      const pct=Math.min(100,Math.round(count/MAX*100)), good=count>=MIN;
      return `<div class="hist-item ${good?'good':'bad'} fade-in">
        <div class="hist-day">${fmt(day)}</div>
        <div class="hist-bar-wrap"><div class="hist-bar ${good?'good':'bad'}" style="width:${pct}%"></div></div>
        <div class="hist-count ${good?'good':'bad'}">${count}</div>
      </div>`;
    }).join('');
  }

  function changeQty(d){ const i=document.getElementById('qty-input'); i.value=Math.max(1,Math.min(200,parseInt(i.value||0)+d)); }

  function confirmToday(){
    const today=getToday();
    if(state.history[today]!==undefined) return;
    const qty=parseInt(document.getElementById('qty-input').value||0);
    if(qty<1) return;
    state.history[today]=qty; save();
    const big=document.getElementById('total-done');
    big.classList.add('glow-pulse');
    setTimeout(()=>big.classList.remove('glow-pulse'),700);
    render();
  }

  function resetData(){
    if(confirm('RESETAR TODOS OS DADOS DE MARÇO?')){ state={history:{}}; save(); render(); }
  }

  render();
</script>
</body>
</html>
